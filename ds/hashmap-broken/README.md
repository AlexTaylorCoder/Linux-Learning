# Things to note

# Hashkey function, uses val + encoding to create
# Hashkey % arr len is used to determine arr ind (key). Mod ensures that hashkey in range of bounds

# Dupe hash
# There can be dupe value -> same key
# so need to check this 
