#include <stdlib.h>
#define EXP_RATE 1.5

typedef struct Node {
	int *val;
	int cap;
	int len;
	Node *next;
} Node;

//Best approach here would be similar to arraylist with expanding cap, but when reach cap last ele
int alloc(Node *self) {
	realloc()	
}

int add(Node *self, int val) {
	if (self -> cap <= self -> len) {
		alloc()	


int main() {
	Node
	//alloc per node
